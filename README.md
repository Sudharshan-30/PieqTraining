# 🌐 Basic Web Development Tasks – Completed

I have successfully completed all the required tasks for **Part 1: Basic Web Development (HTML, CSS, JavaScript)**. Below is a summary of what was implemented:

## ✅ Task 1: Semantic HTML Profile Page

Created a structured profile page using semantic HTML elements:

* Added an `<h1>` heading with my name
* Included a profile image using `<img>`
* Wrote a short self-description using `<p>`
* Listed hobbies/skills using an unordered list `<ul>`

## 🎨 Task 2: Styled Profile Page (CSS)

Enhanced the profile page with CSS styling:

* Changed the background color of the page
* Centered the content using Flexbox
* Added borders and padding to the profile section
* Customized fonts, colors, and overall layout for a clean and modern look

## 🔘 Task 3: Interactive Button (JavaScript)

Implemented interactivity using JavaScript:

* Added a button below the profile
* On click, the background color of the page changes dynamically (random color / toggle effect)

## 📝 Task 4: Simple To-Do List UI

Built a functional to-do list interface:

* Created an input field, "Add Item" button, and an empty list (`<ul>`)
* Added JavaScript logic to append new list items (`<li>`) based on user input

## 📩 Task 5: Form Validation

Developed a simple contact form with validation:

* Included fields for Name and Email
* Used JavaScript to validate inputs on form submission
* Prevented submission if fields are empty using `event.preventDefault()`
* Displayed error messages dynamically to guide the user

---

✨ This project helped me strengthen my understanding of **HTML structure, CSS styling, and JavaScript interactivity**, forming a solid foundation in front-end web development.
